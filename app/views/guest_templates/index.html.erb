<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All guests
    </h1>

    <a href="/guests/new" class="btn btn-block btn-outline-secondary">
      Add a new guest
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "guest_templates/guests_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->
<div class="row mb-3">
  <div class="col-md-12">
    <%= render partial: "guest_templates/all_photos_map" %>
  </div>
</div>
<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          First name
        </th>

        <th>
          Last name
        </th>

        <th>
          Address
        </th>

        <th>
          Email
        </th>

        <th>
          Phone
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @guests.each do |guest| %>
      <tr>
        <!-- Display guest.id start -->
        <td>
          <%= guest.id %>
        </td>
        <!-- Display guest.id end -->

        <!-- Display guest.first_name start -->
        <td>
          <%= guest.first_name %>
        </td>
        <!-- Display guest.first_name end -->

        <!-- Display guest.last_name start -->
        <td>
          <%= guest.last_name %>
        </td>
        <!-- Display guest.last_name end -->

        <!-- Display guest.address start -->
        <td>
          <%= guest.address %>
        </td>
        <!-- Display guest.address end -->

        <!-- Display guest.email start -->
        <td>
          <%= guest.email %>
        </td>
        <!-- Display guest.email end -->

        <!-- Display guest.phone start -->
        <td>
          <%= guest.phone %>
        </td>
        <!-- Display guest.phone end -->

        <!-- Display guest.user_id start -->
        <td>
          <% if guest.user.present? %>
            <a href="/users/<%= guest.user.id %>">
              <%= guest.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display guest.user_id end -->

        <!-- Display guest.created_at start -->
        <td>
          <%= time_ago_in_words(guest.created_at) %> ago
        </td>
        <!-- Display guest.created_at end -->

        <!-- Display guest.updated_at start -->
        <td>
          <%= time_ago_in_words(guest.updated_at) %> ago
        </td>
        <!-- Display guest.updated_at end -->

        <td>
          <a href="/guests/<%= guest.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @guests, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
